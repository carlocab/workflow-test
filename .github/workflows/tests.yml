name: CI
on: push

defaults:
  run:
    shell: bash -xeuo pipefail {0}

permissions:
  contents: read

jobs:
  postgres-probe:
    name: Install Postgres
    strategy:
      matrix:
        runner: [macos-latest, ubuntu-latest]
      fail-fast: false
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@main

      - name: Install Postgres using a Brewfile
        run: |
          echo 'brew "postgresql@18"' >Brewfile
          brew bundle install
