name: CI
on: push

jobs:
  test:
    runs-on: macos-latest
    steps:
      - run: brew install go

      - uses: actions/setup-go@v5

      - run: |
          printf '```\n' >> "${GITHUB_OUTPUT}"
          {
            which -a go
            go env
          } | tee -a "${GITHUB_OUTPUT}"
          printf '```\n' >> "${GITHUB_OUTPUT}"
